<!DOCTYPE html>
<html lang="en" class="dark">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Algora</title>

    <!-- Inter Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <!-- Tailwind CSS via CDN -->
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif']
                    },
                    colors: {
                        primary: {
                            50: '#f5f3ff',
                            100: '#ede9fe',
                            200: '#ddd6fe',
                            300: '#c4b5fd',
                            400: '#a78bfa',
                            500: '#8b5cf6',
                            600: '#7c3aed',
                            700: '#6d28d9',
                            800: '#5b21b6',
                            900: '#4c1d95'
                        }
                    }
                }
            }
        }
    </script>

    <!-- Flowbite CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css" rel="stylesheet" />

    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet" />

    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>

    <style>
        /* Custom scrollbar */
        ::-webkit-scrollbar { width: 4px; height: 4px; }
        ::-webkit-scrollbar-track { background: transparent; }
        ::-webkit-scrollbar-thumb { background: #e2e8f0; border-radius: 2px; }
        ::-webkit-scrollbar-thumb:hover { background: #cbd5e1; }
        .dark ::-webkit-scrollbar-thumb { background: #334155; }
        .dark ::-webkit-scrollbar-thumb:hover { background: #475569; }

        /* Sidebar styling */
        .nav-item {
            transition: all 0.15s ease;
        }
        .nav-item:hover {
            background-color: rgba(139, 92, 246, 0.08);
        }
        .dark .nav-item:hover {
            background-color: rgba(139, 92, 246, 0.15);
        }
        .nav-item.active {
            background-color: rgba(139, 92, 246, 0.12);
            color: #7c3aed;
            font-weight: 500;
        }
        .dark .nav-item.active {
            background-color: rgba(139, 92, 246, 0.2);
            color: #a78bfa;
        }
        .nav-item.active .nav-icon {
            color: #7c3aed;
        }
        .dark .nav-item.active .nav-icon {
            color: #a78bfa;
        }

        /* Dropdown animation */
        .nav-dropdown {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.2s ease-out;
        }
        .nav-dropdown.open {
            max-height: 500px;
        }

        /* Section divider */
        .nav-section {
            font-size: 0.65rem;
            font-weight: 600;
            letter-spacing: 0.08em;
            text-transform: uppercase;
            color: #94a3b8;
            padding: 1rem 1rem 0.5rem;
        }
        .dark .nav-section {
            color: #64748b;
        }
    </style>

    @await RenderSectionAsync("Styles", required: false)
    @await RenderSectionAsync("css", required: false)
</head>
<body class="font-sans antialiased bg-gray-50 dark:bg-gray-900">
    @{
        var currentPath = Context.Request.Path.ToString().ToLower();
        bool IsActive(string path) => currentPath == path.ToLower() || currentPath.StartsWith(path.ToLower() + "/");
        bool IsInSection(string basePath) => currentPath.StartsWith(basePath.ToLower());
    }

    <!-- Mobile Menu Button -->
    <button data-drawer-target="sidebar" data-drawer-toggle="sidebar" aria-controls="sidebar" type="button" class="inline-flex items-center p-2 mt-2 ms-3 text-sm text-gray-500 rounded-lg sm:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400 dark:hover:bg-gray-700 dark:focus:ring-gray-600">
        <span class="sr-only">Open sidebar</span>
        <i class="fas fa-bars text-lg"></i>
    </button>

    <!-- Sidebar -->
    <aside id="sidebar" class="fixed top-0 left-0 z-40 w-60 h-screen transition-transform -translate-x-full sm:translate-x-0" aria-label="Sidebar">
        <div class="h-full flex flex-col bg-gray-50 dark:bg-gray-900 border-r border-gray-200 dark:border-gray-800">
            <!-- Logo -->
            <div class="flex items-center h-16 px-4 border-b border-gray-100 dark:border-gray-800">
                <a href="/dashboard" class="flex items-center gap-3">
                    <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-violet-600 to-purple-600 flex items-center justify-center shadow-lg shadow-purple-500/20">
                        <i class="fas fa-cube text-white text-sm"></i>
                    </div>
                    <span class="text-lg font-semibold text-gray-900 dark:text-white">Algora</span>
                </a>
            </div>

            <!-- Navigation -->
            <nav class="flex-1 overflow-y-auto py-4 px-3">
                <!-- Dashboard -->
                <a href="/dashboard" class="nav-item @(IsActive("/dashboard") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-th-large w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Dashboard</span>
                </a>

                <!-- Commerce Section -->
                <div class="nav-section mt-4">Commerce</div>
                <a href="/orders" class="nav-item @(IsActive("/orders") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-shopping-bag w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Orders</span>
                </a>
                <a href="/products" class="nav-item @(IsActive("/products") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-box w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Products</span>
                </a>
                <a href="/bundles/admin" class="nav-item @(IsActive("/bundles") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-boxes-stacked w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Bundles</span>
                </a>

                <!-- Communication Section -->
                <div class="nav-section mt-4">Communication</div>
                <a href="/communication/email-campaigns" class="nav-item @(IsActive("/communication/email-campaigns") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-envelope w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Email Campaigns</span>
                </a>
                <a href="/communication/sms" class="nav-item @(IsActive("/communication/sms") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-comment-sms w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>SMS</span>
                </a>
                <a href="/communication/whatsapp" class="nav-item @(IsActive("/communication/whatsapp") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fab fa-whatsapp w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>WhatsApp</span>
                </a>

                <!-- Marketing Section -->
                <div class="nav-section mt-4">Marketing</div>
                <button onclick="toggleNav('marketing')" class="nav-item w-full flex items-center justify-between px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <div class="flex items-center gap-3">
                        <i class="nav-icon fas fa-bullhorn w-4 text-gray-400 dark:text-gray-500"></i>
                        <span>Marketing</span>
                    </div>
                    <i id="marketing-chevron" class="fas fa-chevron-right text-[10px] text-gray-400 transition-transform @(IsInSection("/upsell") || IsInSection("/reviews") || IsInSection("/analytics") || IsInSection("/abandoned") ? "rotate-90" : "")"></i>
                </button>
                <div id="marketing-dropdown" class="nav-dropdown @(IsInSection("/upsell") || IsInSection("/reviews") || IsInSection("/analytics") || IsInSection("/abandoned") ? "open" : "") pl-7 space-y-0.5">
                    <a href="/upsell" class="nav-item @(IsActive("/upsell") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Upsell</a>
                    <a href="/reviews/admin" class="nav-item @(IsInSection("/reviews") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Reviews</a>
                    <a href="/analytics" class="nav-item @(IsActive("/analytics") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Analytics</a>
                    <a href="/abandoned-checkouts" class="nav-item @(IsInSection("/abandoned") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Abandoned Carts</a>
                </div>

                <!-- AI Tools Section -->
                <div class="nav-section mt-4">AI Tools</div>
                <a href="/ai" class="nav-item @(currentPath == "/ai" ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-sparkles w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>AI Dashboard</span>
                </a>
                <button onclick="toggleNav('ai')" class="nav-item w-full flex items-center justify-between px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <div class="flex items-center gap-3">
                        <i class="nav-icon fas fa-wand-magic-sparkles w-4 text-gray-400 dark:text-gray-500"></i>
                        <span>AI Tools</span>
                    </div>
                    <i id="ai-chevron" class="fas fa-chevron-right text-[10px] text-gray-400 transition-transform @(IsInSection("/ai/") ? "rotate-90" : "")"></i>
                </button>
                <div id="ai-dropdown" class="nav-dropdown @(IsInSection("/ai/") ? "open" : "") pl-7 space-y-0.5">
                    <a href="/ai/descriptions" class="nav-item @(IsActive("/ai/descriptions") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Descriptions</a>
                    <a href="/ai/seo" class="nav-item @(IsActive("/ai/seo") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">SEO Optimizer</a>
                    <a href="/ai/pricing" class="nav-item @(IsActive("/ai/pricing") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Pricing AI</a>
                    <a href="/ai/alttext" class="nav-item @(IsActive("/ai/alttext") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Alt Text</a>
                    <a href="/ai/chatbot" class="nav-item @(IsActive("/ai/chatbot") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Chatbot</a>
                </div>

                <!-- Operations Section -->
                <div class="nav-section mt-4">Operations</div>
                <a href="/operations" class="nav-item @(currentPath == "/operations" ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-gauge-high w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Overview</span>
                </a>
                <button onclick="toggleNav('ops')" class="nav-item w-full flex items-center justify-between px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <div class="flex items-center gap-3">
                        <i class="nav-icon fas fa-truck w-4 text-gray-400 dark:text-gray-500"></i>
                        <span>Supply Chain</span>
                    </div>
                    <i id="ops-chevron" class="fas fa-chevron-right text-[10px] text-gray-400 transition-transform @(IsInSection("/operations/") ? "rotate-90" : "")"></i>
                </button>
                <div id="ops-dropdown" class="nav-dropdown @(IsInSection("/operations/") ? "open" : "") pl-7 space-y-0.5">
                    <a href="/operations/suppliers" class="nav-item @(IsActive("/operations/suppliers") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Suppliers</a>
                    <a href="/operations/purchaseorders" class="nav-item @(IsActive("/operations/purchaseorders") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Purchase Orders</a>
                    <a href="/operations/locations" class="nav-item @(IsActive("/operations/locations") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Locations</a>
                    <a href="/operations/barcodes" class="nav-item @(IsActive("/operations/barcodes") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Barcodes</a>
                    <a href="/operations/packing-slips" class="nav-item @(IsActive("/operations/packing-slips") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Packing Slips</a>
                    <a href="/operations/label-designer" class="nav-item @(IsActive("/operations/label-designer") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Label Designer</a>
                </div>

                <!-- Inventory Section -->
                <div class="nav-section mt-4">Inventory</div>
                <a href="/inventory" class="nav-item @(currentPath == "/inventory" ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-chart-line w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Predictions</span>
                </a>
                <a href="/inventory/alerts" class="nav-item @(IsActive("/inventory/alerts") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-bell w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Alerts</span>
                </a>
                <a href="/inventory/settings" class="nav-item @(IsActive("/inventory/settings") ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-sliders w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Settings</span>
                </a>

                <!-- Customer Hub Section -->
                <div class="nav-section mt-4">Customer Hub</div>
                <a href="/customer-hub" class="nav-item @(currentPath == "/customer-hub" ? "active" : "") flex items-center gap-3 px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-users w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Overview</span>
                </a>
                <button onclick="toggleNav('hub')" class="nav-item w-full flex items-center justify-between px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <div class="flex items-center gap-3">
                        <i class="nav-icon fas fa-headset w-4 text-gray-400 dark:text-gray-500"></i>
                        <span>Support</span>
                    </div>
                    <i id="hub-chevron" class="fas fa-chevron-right text-[10px] text-gray-400 transition-transform @(IsInSection("/customer-hub/") ? "rotate-90" : "")"></i>
                </button>
                <div id="hub-dropdown" class="nav-dropdown @(IsInSection("/customer-hub/") ? "open" : "") pl-7 space-y-0.5">
                    <a href="/customer-hub/inbox" class="nav-item @(IsActive("/customer-hub/inbox") && !currentPath.Contains("live-chat") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Unified Inbox</a>
                    <a href="/customer-hub/inbox/live-chat" class="nav-item @(IsActive("/customer-hub/inbox/live-chat") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Live Chat</a>
                    <a href="/customer-hub/exchanges" class="nav-item @(IsActive("/customer-hub/exchanges") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Exchanges</a>
                    <a href="/customer-hub/loyalty" class="nav-item @(IsActive("/customer-hub/loyalty") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Loyalty Program</a>
                </div>

                <!-- Returns -->
                <a href="/returns" class="nav-item @(IsInSection("/returns") ? "active" : "") flex items-center gap-3 px-3 py-2 mt-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <i class="nav-icon fas fa-rotate-left w-4 text-gray-400 dark:text-gray-500"></i>
                    <span>Returns</span>
                </a>

                <!-- Admin Section -->
                <div class="nav-section mt-4">Admin</div>
                <button onclick="toggleNav('admin')" class="nav-item w-full flex items-center justify-between px-3 py-2 rounded-lg text-sm text-gray-700 dark:text-gray-300">
                    <div class="flex items-center gap-3">
                        <i class="nav-icon fas fa-shield-halved w-4 text-gray-400 dark:text-gray-500"></i>
                        <span>Administration</span>
                    </div>
                    <i id="admin-chevron" class="fas fa-chevron-right text-[10px] text-gray-400 transition-transform @(IsInSection("/admin") ? "rotate-90" : "")"></i>
                </button>
                <div id="admin-dropdown" class="nav-dropdown @(IsInSection("/admin") ? "open" : "") pl-7 space-y-0.5">
                    <a href="/admin/clients" class="nav-item @(IsActive("/admin/clients") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Clients</a>
                    <a href="/admin/plan-features" class="nav-item @(IsActive("/admin/plan-features") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Plan Features</a>
                    <a href="/admin/plan-requests" class="nav-item @(IsActive("/admin/plan-requests") ? "active" : "") flex items-center gap-3 px-3 py-1.5 rounded-lg text-sm text-gray-600 dark:text-gray-400">Plan Requests</a>
                </div>
            </nav>

            <!-- User Section -->
            <div class="border-t border-gray-100 dark:border-gray-800 p-3">
                <div class="flex items-center gap-3 px-2 py-2">
                    <div class="w-8 h-8 rounded-full bg-gradient-to-br from-violet-500 to-purple-600 flex items-center justify-center text-white text-sm font-medium shadow-lg shadow-purple-500/20">
                        A
                    </div>
                    <div class="flex-1 min-w-0">
                        <p class="text-sm font-medium text-gray-900 dark:text-white truncate">Admin User</p>
                        <p class="text-xs text-gray-500 dark:text-gray-400 truncate">admin@algora.com</p>
                    </div>
                    <a href="/account/profile" class="p-1.5 text-gray-400 hover:text-gray-600 dark:hover:text-gray-300 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-800">
                        <i class="fas fa-cog text-sm"></i>
                    </a>
                </div>
            </div>
        </div>
    </aside>

    <!-- Main Content -->
    <div class="sm:ml-60">
        <!-- Top Navbar -->
        <nav class="sticky top-0 z-30 bg-white/80 dark:bg-gray-900/80 backdrop-blur-md border-b border-gray-200 dark:border-gray-800">
            <div class="flex items-center justify-between h-14 px-4">
                <!-- Breadcrumb -->
                <div class="flex items-center">
                    <nav class="flex" aria-label="Breadcrumb">
                        <ol class="inline-flex items-center space-x-2">
                            <li class="inline-flex items-center">
                                <a href="/dashboard" class="inline-flex items-center text-sm text-gray-500 hover:text-violet-600 dark:text-gray-400 dark:hover:text-violet-400">
                                    <i class="fas fa-home text-xs me-2"></i>
                                    Home
                                </a>
                            </li>
                            @if (ViewData["Breadcrumb"] != null)
                            {
                                <li>
                                    <div class="flex items-center">
                                        <i class="fas fa-chevron-right text-[10px] text-gray-400 mx-2"></i>
                                        <span class="text-sm font-medium text-gray-700 dark:text-gray-300">@ViewData["Breadcrumb"]</span>
                                    </div>
                                </li>
                            }
                        </ol>
                    </nav>
                </div>

                <!-- Right side buttons -->
                <div class="flex items-center gap-2">
                    <!-- Dark mode toggle -->
                    <button id="theme-toggle" type="button" class="p-2 text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800 rounded-lg transition-colors">
                        <i id="theme-toggle-dark-icon" class="hidden fas fa-moon text-sm"></i>
                        <i id="theme-toggle-light-icon" class="hidden fas fa-sun text-sm"></i>
                    </button>

                    <!-- Notifications -->
                    <button type="button" class="relative p-2 text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800 rounded-lg transition-colors">
                        <i class="fas fa-bell text-sm"></i>
                        <span class="absolute top-1 right-1 w-2 h-2 bg-red-500 rounded-full"></span>
                    </button>

                    <!-- Help -->
                    <button type="button" class="p-2 text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-800 rounded-lg transition-colors">
                        <i class="fas fa-question-circle text-sm"></i>
                    </button>
                </div>
            </div>
        </nav>

        <!-- Page Content -->
        <main class="p-6">
            @RenderBody()
        </main>
    </div>

    <!-- Modal container -->
    @await RenderSectionAsync("Modals", required: false)

    <!-- Flowbite JS -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"></script>

    <!-- Scripts -->
    <script>
        // Theme toggle
        if (localStorage.getItem('color-theme') === 'dark' || (!('color-theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
            document.getElementById('theme-toggle-light-icon').classList.remove('hidden');
        } else {
            document.documentElement.classList.remove('dark');
            document.getElementById('theme-toggle-dark-icon').classList.remove('hidden');
        }

        document.getElementById('theme-toggle').addEventListener('click', function() {
            document.getElementById('theme-toggle-dark-icon').classList.toggle('hidden');
            document.getElementById('theme-toggle-light-icon').classList.toggle('hidden');

            if (document.documentElement.classList.contains('dark')) {
                document.documentElement.classList.remove('dark');
                localStorage.setItem('color-theme', 'light');
            } else {
                document.documentElement.classList.add('dark');
                localStorage.setItem('color-theme', 'dark');
            }
        });

        // Navigation dropdown toggle
        function toggleNav(name) {
            const dropdown = document.getElementById(name + '-dropdown');
            const chevron = document.getElementById(name + '-chevron');

            if (dropdown.classList.contains('open')) {
                dropdown.classList.remove('open');
                chevron.classList.remove('rotate-90');
            } else {
                dropdown.classList.add('open');
                chevron.classList.add('rotate-90');
            }
        }
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
